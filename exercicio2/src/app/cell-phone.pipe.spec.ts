import { CellPhonePipe } from './cell-phone.pipe';

describe('CellPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new CellPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
